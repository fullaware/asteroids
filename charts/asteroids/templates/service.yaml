apiVersion: v1
kind: Service
metadata:
  name: {{ include "asteroids.fullname" . }}
  labels:
    {{- include "asteroids.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "asteroids.selectorLabels" . | nindent 4 }}
